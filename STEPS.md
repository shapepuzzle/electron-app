# Steps used to create this app

1. npm init

2. npm install electron --save-dev

3. npm install --save-dev @electron-forge/cli

4. npx electron-forge import

5. npm install --save-dev @electron-forge/maker-dmg
